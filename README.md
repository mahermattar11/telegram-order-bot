# **OrderlyBot - نظام إدارة الطلبات الذكي** 🤖

![Python](https://img.shields.io/badge/Python-3.11-blue)
![Flask](https://img.shields.io/badge/Flask-3.0-green)
![Telegram](https://img.shields.io/badge/Telegram-Bot-blue)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-Database-blue)
![Render](https://img.shields.io/badge/Render-Deployed-purple)

## **📖 نظرة عامة**

OrderlyBot هو نظام متكامل لإدارة الطلبات عبر **Telegram** مع لوحة تحكم ويب متكاملة. النظام مصمم خصيصاً للتجار وأصحاب المحلات لإدارة طلبات عملائهم بسهولة وكفاءة.

### **🎯 المميزات الرئيسية**
- **بوت Telegram** لاستقبال الطلبات من العملاء
- **لوحة تحكم ويب** لإدارة الطلبات والتقارير
- **قاعدة بيانات PostgreSQL** لتخزين البيانات
- **استضافة مجانية** على Render (24/7)
- **واجهة عربية كاملة** مع تصميم RTL
- **متجاوب مع جميع الأجهزة** (جوال، تابلت، حاسوب)

---

## **🚀 بدء الاستخدام**

### **المتطلبات الأساسية**
- حساب على [Telegram](https://telegram.org) لإنشاء البوت
- حساب على [GitHub](https://github.com) لرفع الكود
- حساب على [Render](https://render.com) للاستضافة
- Python 3.11 أو أعلى

---

## **⚙️ التثبيت والتشغيل**

### **1. التثبيت المحلي (للتطوير)**

```bash
# 1. استنساخ المشروع
git clone https://github.com/yourusername/telegram-order-bot.git
cd telegram-order-bot

# 2. إنشاء بيئة افتراضية
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# 3. تثبيت المكتبات
pip install -r requirements.txt

# 4. تشغيل التطبيق
python OrderlyBot.py
```

### **2. ملفات المشروع الرئيسية**

```
telegram_order_bot/
├── static/                    # ملفات الثابتة
│   ├── css/
│   │   └── style.css         # أنماط التصميم
│   └── js/
│       ├── main.js           # JavaScript الرئيسي
│       └── orders.js         # JavaScript للطلبات
├── templates/                 # قوالب HTML
│   ├── dashboard.html        # لوحة التحكم الرئيسية
│   ├── orders.html           # صفحة الطلبات
│   ├── login.html            # صفحة تسجيل الدخول
│   └── settings.html         # صفحة الإعدادات
├── OrderlyBot.py             # الملف الرئيسي
├── requirements.txt          # مكتبات Python
├── render.yaml               # إعدادات Render
├── runtime.txt               # إصدار Python
└── README.md                 # هذا الملف
```

---

## **🔧 إعدادات البيئة**

### **متغيرات البيئة المطلوبة**

أضف هذه المتغيرات في Render أو في ملف `.env` للتطوير المحلي:

```env
DATABASE_URL=postgresql://user:pass@host:port/dbname
BOT_TOKEN=1234567890:ABCdefGhIJKlmnOpQRStuVWxyZ
SECRET_KEY=your-secret-key-here
```

### **كيفية الحصول على BOT_TOKEN:**
1. افتح Telegram وابحث عن **@BotFather**
2. أرسل `/newbot`
3. اتبع التعليمات واحصل على التوكن

---

## **🌐 النشر على Render**

### **1. رفع المشروع إلى GitHub**
```bash
git add .
git commit -m "Initial commit"
git push origin main
```

### **2. النشر على Render**
1. سجل دخول إلى [render.com](https://render.com)
2. اختر **New Web Service**
3. اختر مستودع GitHub الخاص بك
4. استخدم الإعدادات التالية:
   - **Name:** `orderlybot` (أو أي اسم تريده)
   - **Runtime:** Python 3.11
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python OrderlyBot.py`

### **3. إضافة قاعدة بيانات**
1. في Render Dashboard، اختر **New PostgreSQL**
2. اختر **Free Plan**
3. اربطها مع خدمتك الرئيسية

---

## **📱 استخدام النظام**

### **1. للعملاء (على Telegram)**
1. ابحث عن البوت على Telegram
2. أرسل `/start`
3. اتبع التعليمات:
   - اختر اللغة
   - اختر الفئة
   - اختر المنتج
   - أدخل: الاسم، الهاتف، العنوان
4. سيتم حفظ الطلب وإرسال إشعار للتاجر

### **2. للتجار (لوحة التحكم)**
1. افتح: `https://your-app.onrender.com`
2. سجل الدخول:
   - **اسم المستخدم:** `admin`
   - **كلمة المرور:** `admin123`
3. الوظائف المتاحة:
   - عرض الطلبات الجديدة
   - تغيير حالة الطلب
   - التواصل مع العملاء
   - عرض التقارير والإحصائيات

---

## **🔌 واجهة برمجة التطبيقات (API)**

### **Endpoints المتاحة**

| الطريق | الطريقة | الوصف |
|--------|---------|--------|
| `/api/orders/<id>/status` | POST | تغيير حالة الطلب |
| `/api/orders/<id>` | DELETE | حذف طلب |
| `/api/stats` | GET | إحصائيات الطلبات |
| `/api/orders/new/count` | GET | عدد الطلبات الجديدة |
| `/api/orders/export/excel` | GET | تصدير الطلبات لـ Excel |

### **مثال: تغيير حالة الطلب**
```javascript
fetch('/api/orders/123/status', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({status: 'completed'})
})
```

---

## **🎨 التصميم والمكونات**

### **المكتبات المستخدمة**
- **Bootstrap 5** - إطار عمل CSS
- **Bootstrap Icons** - الأيقونات
- **Cairo Font** - خط عربي
- **SweetAlert2** - إشعارات جميلة
- **Chart.js** (مخطط للاستخدام المستقبلي) - الرسوم البيانية

### **الأنماط المخصصة**
- تصميم RTL كامل
- ألوان مخصصة (`--primary-color: #4361ee`)
- كروت وإحصائيات متحركة
- متجاوب مع جميع أحجام الشاشات

---

## **📊 الهيكل التقني**

### **قاعدة البيانات (PostgreSQL)**

#### **جدول الطلبات (`orders`):**
```sql
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    category VARCHAR(20) NOT NULL,
    product TEXT NOT NULL,
    customer_name TEXT NOT NULL,
    phone TEXT NOT NULL,
    address TEXT NOT NULL,
    quantity TEXT NOT NULL,
    size TEXT,
    language VARCHAR(5) DEFAULT 'ar',
    status VARCHAR(20) DEFAULT 'new',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    merchant_id INTEGER DEFAULT 1
);
```

#### **جدول التجار (`merchants`):**
```sql
CREATE TABLE merchants (
    id SERIAL PRIMARY KEY,
    telegram_id BIGINT UNIQUE,
    username VARCHAR(100),
    business_name TEXT,
    plan VARCHAR(20) DEFAULT 'trial',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## **🔒 الأمان**

### **الإجراءات الأمنية المطبقة**
- تسجيل دخول باستخدام Flask-Login
- كلمات مرور مشفرة باستخدام Werkzeug
- جلسات آمنة مع SECRET_KEY
- حماية من CSRF
- وصول مقيد بـ `@login_required`

### **توصيات أمان إضافية**
1. تغيير كلمة المرور الافتراضية (`admin123`)
2. استخدام HTTPS (مفعل تلقائياً على Render)
3. تحديث المكتبات بانتظام
4. نسخ احتياطي لقاعدة البيانات

---

## **🚀 النشر والتوسع**

### **للعملاء المتعددين**
1. **نسخ المشروع** لكل عميل جديد
2. **تعديل البيانات** (الاسم، المنتجات، الشعار)
3. **إنشاء بوت Telegram** منفصل لكل عميل
4. **نشر نسخة مستقلة** على Render

### **مثال: تكلفة التشغيل الشهرية**
| المورد | الخطة | السعر |
|--------|-------|-------|
| **Render Web Service** | Free Plan | $0 |
| **PostgreSQL Database** | Free Plan | $0 |
| **نطاق مخصص (Custom Domain)** | اختياري | $10-15 |

---

## **🔧 استكشاف الأخطاء وإصلاحها**

### **مشاكل شائعة وحلولها**

#### **1. البوت لا يستجيب**
```bash
# تحقق من:
- BOT_TOKEN صحيح
- البوت ليس مشغولاً في مكان آخر
- انتظر 120 ثانية بعد إعادة التشغيل
```

#### **2. خطأ في قاعدة البيانات**
```bash
# تحقق من:
- DATABASE_URL صحيح
- PostgreSQL يعمل على Render
- الجداول مخلوقة (افتح /init-db)
```

#### **3. لوحة التحكم لا تعمل**
```bash
# تحقق من:
- SECRET_KEY مضبوط
- ملفات templates موجودة
- تم تسجيل الدخول بـ admin/admin123
```

### **أوامر مفيدة للتصحيح**
```bash
# فحص صحة التطبيق
curl https://your-app.onrender.com/health

# فحص قاعدة البيانات
curl https://your-app.onrender.com/test-db

# رؤية الـLogs على Render
# اذهب لـ Dashboard → اختر خدمتك → Logs
```

---

## **📈 خطط التطوير المستقبلية**

### **الميزات المخطط لها**
- [ ] نظام نقاط للعملاء
- [ ] تقييمات ومراجعات
- [ ] إشعارات عبر الواتساب
- [ ] تطبيق جوال للتجار
- [ ] دفع إلكتروني
- [ ] تقارير متقدمة مع رسوم بيانية

### **التطوير الحالي**
- [x] بوت Telegram أساسي
- [x] لوحة تحكم ويب
- [x] قاعدة بيانات PostgreSQL
- [x] واجهة API
- [x] تصميم متجاوب
- [x] نظام تسجيل دخول

---

## **🤝 المساهمة في المشروع**

### **كيفية المساهمة**
1. Fork المشروع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

### **توجيهات للمساهمين**
- اكتب تعليقات واضحة في الكود
- اختبر التغييرات محلياً قبل الرفع
- اتبع تنسيق الكود الموجود
- أضف وثائق للميزات الجديدة

---

## **📄 الرخصة**

هذا المشروع مرخص تحت رخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل.

---

## **📞 الدعم والاتصال**

### **للإبلاغ عن مشاكل**
1. افتح [Issue](https://github.com/yourusername/telegram-order-bot/issues) على GitHub
2. صف المشكلة بوضوح
3. أرفق لقطات شاشة إذا أمكن
4. اذكر خطوات إعادة المشكلة

### **للأسئلة والاستفسارات**
- **البريد الإلكتروني:** your-email@example.com
- **Telegram:** @yourtelegram
- **GitHub Issues:** [هنا](https://github.com/yourusername/telegram-order-bot/issues)

---

## **🌟 شكر وتقدير**

### **المساهمون**
- **[اسمك]** - المطور الرئيسي
- **[أسماء المساهمين]** - المساهمون

### **المكتبات والشكر**
- [Flask](https://flask.palletsprojects.com/) - إطار عمل ويب بسيط وقوي
- [python-telegram-bot](https://python-telegram-bot.org/) - مكتبة Telegram Bot
- [Bootstrap](https://getbootstrap.com/) - إطار عمل CSS
- [Render](https://render.com) - منصة الاستضافة المجانية

---

## **🎉 بدء الاستخدام الآن!**

### **للبدء السريع:**
```bash
# 1. انسخ المشروع
git clone https://github.com/yourusername/telegram-order-bot.git

# 2. اتبع خطوات التثبيت
# 3. عدل البيانات لتناسب متجرك
# 4. انشر على Render
# 5. ابدأ باستقبال الطلبات!
```

### **للعملاء:**
ابدأ ببيع منتجاتك عبر Telegram اليوم! 🚀

### **للمطورين:**
ساهم في المشروع أو استخدمه كنقطة بداية لمشاريعك الخاصة! 💻

---

**✨ تم تطوير OrderlyBot بحب لمساعدة التجار على النمو الرقمي ✨**